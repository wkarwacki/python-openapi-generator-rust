{{fmtName @key}}: {{#if (eq this.type "obj")}}{{fmtOptIfNeeded (add (add prefix (fmtClass @key this.origin)) suffix) this.opt}}{{else}}{{#if (and this.path this.origin)}}{{fmtOptIfNeeded (add (add this.origin ".") (fmtType this)) this.opt}}{{else if (eq type "enum")}}{{fmtOptIfNeeded (add prefix (fmtClass @key)) this.opt}}{{else}}{{fmtOptIfNeeded (fmtType this (add (add prefix (fmtClass @key)) ".Companion")) this.opt}}{{/if}}{{/if}}
