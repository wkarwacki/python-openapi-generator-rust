=== must have

=== gen

=== other

=== known-bugs/limitations:
tristate (opt/null) is not handled corretly in any gen
python-gen: ref to separate def (with inner ref to def) in multipart/form-data case - workaround = do not use inner refs when defining multipart/form-data
