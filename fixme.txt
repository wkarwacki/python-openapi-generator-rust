=== must have
do not require trust main module, respect passed module always
make all tests work

=== gen
python: type_mapping -> make it work for both client and server
python-client: handle ref to separate def (with inner ref to def) in multipart/form-data case
python-client: drop exclude-none and exclude one by one, or make sure that nullable properties are not excluded somehow

=== other
make sure that adt and params are handled recursively